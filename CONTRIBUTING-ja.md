# 仕様を変更する方法

ようこそ！このドキュメントは、素晴らしい新機能を追加したいときに、仕様をどのように安全に変更すればよいかについてのメタな議論をまとめたものです。

私たちは皆、物事をより良くしようとしていることを忘れないでください。互いへの敬意、配慮、親切心、そしてユーモアによって、このプロセスは[楽しく](00-introduction.md#theme-song)、やりがいのあるものになってきました。これからもそうあり続けたいと思っています。私たちはフレンドリーです！

## 拡張設計

仕様にはいくつかの拡張メカニズムが用意されています。まずはそれらを利用することを検討し、必要であれば新しい仕組みを導入してください。

### 新しいピア間メッセージの追加

未知の奇数番号のピア間メッセージは無視されます。すなわち「奇数 (odd) であることは問題ない！(it's OK to be odd!)」ということです。これは、私のことをよく知るほどに納得できるはずです。

追加するメッセージが拡張的なもので、相手側がサポートしているかどうかを知る必要がない場合は、奇数番号を割り当ててください。相手側がサポートしていないと動作が壊れてしまうメッセージ (つまり「決して起きてはならない」もの) には偶数番号を割り当ててください。とはいえミスは起こりうるものですし、将来のソフトウェアバージョンが古いバージョンに対してテストされない可能性も十分にあります。

新しい[メッセージタイプ](01-messaging.md#lightning-message-format)を内部的に試したい場合は、32768 以上を使うことをお勧めします (偶数を使い、誤って外部に流出した場合に必ず壊れるようにしておきます)。

### 新しい機能ビットの追加

[機能ビット](01-messaging.md#the-init-message)は、あるメッセージを送信してよいかを判断する手段であり (上記参照)、適切なピアを見つけるためにも使われます。

機能ビットは、必須化されることが想定されない場合であっても、常にペアで割り当てられます。機能ビットは PR のタイトルで自己割り当てするルールになっており、他の人も同様に自己割り当てしやすくなっています。PR が仕様にマージされるまでの間、実験的な実装は提案されている機能ビットに +100 した値を使うべきです。マージされた後は (プロトコルが変更されていなければ) 両方の機能ビットを受け入れるようにできます。

ほとんどの仕様変更には、対応する機能ビットを必ず用意すべきです。過去には機能ビットをまとめて 1 つに割り当てていましたが、ある実装が壊れていることが分かった際に、特定の機能だけを無効化できないという問題が起きました。

通常、機能ビットは最初に投入されるときは奇数で、その後ほぼすべての実装に普及した段階で偶数になることがあります。これにより、その機能を扱えないピアと通信する可能性がなくなり、レガシーコードを削除できる場合が多くなります。

新しい機能ビットを内部的に試したい場合は、100 以上を使うことをお勧めします。

### ピア間メッセージの拡張

仕様では、メッセージ内の追加データは無視されると定められており、これも将来の拡張手段の 1 つです。BOLT 1.0 では、オプションフィールドはメッセージの末尾に追加され、その存在は機能ビットによって示されていました。

現在推奨されているやり方は、メッセージ末尾に TLV を追加する方法です。TLV にはオプションフィールドを格納できます。ここでも偶数は機能ビットでサポートが示されている場合にのみ送信し、奇数は古いピアに送っても問題ありません (ピアが無条件に送信できるため、実装が容易になることが多いです)。

## 仕様書の作成

仕様書は、テキスト形式で読みやすく、HTML に変換した後も読みやすく、さらに [tools/extract-formats.py] が解析できる形であることが求められます。特にフィールドは適切な型を用い、構造を可能な限り明示的に記述してください (`100*byte` のようなフィールドは避けてください)。

特殊なフォーマット変更が必要な場合は、必要に応じてそのツール自体を修正してもかまいません。

このツールの出力は複数の実装でコード生成に使われます。また、実装側では仕様を積極的に引用し、その引用が正しいことを自動テストで保証することも推奨されます (たとえば [c-lightning がやっているように](https://github.com/ElementsProject/lightning/blob/master/tools/check-bolt.c))。

新しいものが既存のものを置き換える場合は、既存の記述を必ず Legacy サブセクションへ移動してください。新しい読者は最新版に直接たどり着きたいはずです。古典的な Linux snprintf 1.27 マンページのような書き方は真似しないでください:

    RETURN VALUE
       If the output was truncated, the return value is -1, otherwise it is the
       number of characters stored, not including the terminating null.   (Thus
       until  glibc  2.0.6.  Since glibc 2.1 these functions return the  number
       of characters (excluding the trailing null) which would have been  writ‐
       ten to the final string if enough space had been available.)

最初の 1 文だけを読んで「答えはこれだ」と思い込んでしまった人の苦々しさを想像してみてください。20 年経ってもその恨みを忘れず、「こう書いてはいけない」という反面教師として太古の文書を引っ張り出す人が出てくる。そんなのは悲しいことですよね。

書式の詳細を知りたい場合は[詳細なスタイルガイド](.copy-edit-stylesheet-checklist.md)を参照してください。また、[CI システム](.travis.yml) でスペルチェッカーを動かしているので、必要に応じて [.aspell.en.pws] に単語を追加してください。

### 要件 (Requirements) の書き方

要件には自明なものもあれば、微妙なものもあります。要件は、実装者が書くべきコードを順を追って導くように設計されているため、自分自身の実装を開発しながら書いてください。`MUST` / `SHOULD` / `MAY` および `NOT` を使い分けてください。詳しくは [RFC 2119](https://www.ietf.org/rfc/rfc2119.txt) を参照してください。

要件は、実装と同じく writer (書き手) と reader (読み手) に分けて記述します。writer が何をしなければならないかを正確に定義し、writer がそれを行わなかった場合に reader が何をしなければならないかも正確に定義してください。開発者が writer の要件から reader の要件を推測しなければならない、という状況は避けるべきです。

データそのものに要件を書かないでください。たとえば `foo MUST be 0` と書くのではなく、`The writer MUST set foo to 0` と書き、さらに `The reader MUST fail the connection if foo is not 0` と書いてください。

`MUST check` という表現は避けてください。代わりに `MUST fail the connection if`、`MUST fail the channel if`、`MUST send an error message if` のように書いてください。

将来の拡張のために覚えておきたい微妙なテクニックがあります。たとえば `a writer MUST set foo to 0` とだけ書いて reader 側には何も書かないのではなく、`a reader MUST ignore foo` と明示しておく方が望ましいです。仕様の将来のバージョンで writer が `foo` を `1` に設定するケースが定義されたとしても、古い reader はそれを無視することが保証されているからです。

`MAY` は、その項目が何のために存在するのかを示すヒントです。そもそも仕様に書かれていないことは実装が自由に行ってよいので、`MAY` は単なる注釈に近い意味を持ちます。`MUST` は、それを守らないとプロトコルやセキュリティが壊れる場合に使ってください。

要件は曖昧でもかまいません (例: 「適切な時間内に (in a timely manner)」)。ただしそれは、最後の手段として「降参して曖昧にする」場合だけです。あなた自身が分かっていないのなら、哀れな実装者にどんな希望があるでしょうか？

### テストベクトルの作成

新しい低レベルなプロトコル構造には、テストベクトルが必須です。従来は仕様書本文に直接記述されてきましたが、最近は JSON 形式で別ファイルに切り出すのが主流です。実装が機械的に読み取れるようにすることが狙いです。

新しいピア間メッセージについては、対話全体をシミュレートするテストフレームワークが開発中です。

## 仕様変更プロセス

大きな機能については [Delving Bitcoin](https://delvingbitcoin.org/tag/lightning) で議論されます。明示的な issue や pull request は [GitHub リポジトリ](https://github.com/lightning/bolts) で扱います。また、定期的な Lightning 仕様ミーティングが開催されており、開催日時は GitHub リポジトリにピン留めされた issue で告知されます。以前の議論には `lightning-dev` メーリングリストが使われており、そのアーカイブは[こちら](https://www.mail-archive.com/lightning-dev@lists.linuxfoundation.org/)で参照できます。

スペルミス・タイプミス・フォーマットの修正は、2 名のコントリビューターが ack し、nack がなければマージされます。それ以外の変更はすべて、仕様ミーティングで承認・議事録化されます。プロトコルの変更には、相互運用に成功した 2 つの独立した実装が必要です。仕様変更は後から修正するのが難しいため、合意形成には時間がかかることがあります。気長にお待ちください。

加えて、招待制の対面サミットが不定期に開催され、そこで大きな方向性が決まります。これらは素晴らしい場ですので、ぜひ一度参加してみてください。

皆さんが私たちに加わってくれる日を楽しみにしています！
親愛なる Lightning 開発者一同より。
