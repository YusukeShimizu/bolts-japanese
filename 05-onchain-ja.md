# BOLT #5: オンチェーン取引処理の推奨事項

## 概要

Lightning は、2 つの当事者（ローカルノードとリモートノード）がオフチェーンで取引を行うことを可能にします。両者にはそれぞれ*相互署名されたコミットメントトランザクション*が渡され、これがチャネルの現在の状態（基本的には現在の残高）を表します。この*コミットメントトランザクション*は支払いが行われるたびに更新され、いつでもブロックチェーンに公開できる状態になっています。

チャネルが終了する経路には次の 3 つがあります（原文では good / bad / ugly と表現されています）：

1. 良い結末（*相互クローズ*）：ある時点で、ローカルノードとリモートノードがチャネルを閉じることに合意します。両者は*クローズトランザクション*（コミットメントトランザクションに似ていますが、保留中の支払いは含まれません）を生成し、ブロックチェーンに公開します（[BOLT #2: Channel Close](02-peer-protocol.md#channel-close) を参照）。
2. 悪い結末（*一方的クローズ*）：何かがうまくいかなくなったケースで、必ずしも当事者に悪意があるわけではありません。たとえば一方がクラッシュしただけかもしれません。この場合、片方が*最新のコミットメントトランザクション*を公開します。
3. 醜い結末（*取り消されたトランザクションによるクローズ*）：当事者の一方が故意に不正を試み、*古いコミットメントトランザクション*（自分にとって有利な以前のバージョン）を公開します。

Lightning はトラストレスに設計されているため、これら 3 つのいずれの場合でも、状況が適切に処理されるかぎり資金を失うリスクはありません。本ドキュメントの目的は、オンチェーンで上記の状況に遭遇したときにノードがどのように振る舞うべきかを正確に示すことです。

# 目次
  * [一般的な命名法](#general-nomenclature)
  * [コミットメントトランザクション](#commitment-transaction)
  * [チャネルの失敗](#failing-a-channel)
  * [相互クローズの処理](#mutual-close-handling)
  * [一方的クローズの処理：ローカルコミットメントトランザクション](#unilateral-close-handling-local-commitment-transaction)
      * [HTLC 出力の処理：ローカルコミットメント、ローカルオファー](#htlc-output-handling-local-commitment-local-offers)
      * [HTLC 出力の処理：ローカルコミットメント、リモートオファー](#htlc-output-handling-local-commitment-remote-offers)
  * [一方的クローズの処理：リモートコミットメントトランザクション](#unilateral-close-handling-remote-commitment-transaction)
      * [HTLC 出力の処理：リモートコミットメント、ローカルオファー](#htlc-output-handling-remote-commitment-local-offers)
      * [HTLC 出力の処理：リモートコミットメント、リモートオファー](#htlc-output-handling-remote-commitment-remote-offers)
  * [取り消されたトランザクションクローズの処理](#revoked-transaction-close-handling)
	  * [ペナルティトランザクションの重量計算](#penalty-transactions-weight-calculation)
  * [HTLC トランザクションの生成](#generation-of-htlc-transactions)
  * [一般的な要件](#general-requirements)
  * [付録 A: 期待される重量](#appendix-a-expected-weights)
	* [`to_local` ペナルティトランザクションの証人の期待される重量](#expected-weight-of-the-to-local-penalty-transaction-witness)
	* [`offered_htlc` ペナルティトランザクションの証人の期待される重量](#expected-weight-of-the-offered-htlc-penalty-transaction-witness)
	* [`accepted_htlc` ペナルティトランザクションの証人の期待される重量](#expected-weight-of-the-accepted-htlc-penalty-transaction-witness)
  * [著者](#authors)

# 一般的な命名法

未使用の出力は *未解決* と見なされ、本ドキュメントに記載される手順で *解決* されます。通常は別の *解決トランザクション* でその出力を消費することによって行いますが、後でウォレットで使うために出力を記録しておくだけで十分なこともあります。その場合、その出力を含むトランザクション自体が *解決トランザクション* と見なされます。

*解決* された出力は、リモート側の *解決トランザクション* が最も作業量の多いブロックチェーン上で 100 ブロック以上深く取り込まれた時点で *取り返しのつかない形で解決された* と見なされます。100 ブロックは既知の最長の Bitcoin フォークよりはるかに長く、マイナーの報酬の確認に用いられる待ち時間と同じです（[Reference Implementation](https://github.com/bitcoin/bitcoin/blob/4db82b7aab4ad64717f742a7318e3dc6811b41be/src/consensus/tx_verify.cpp#L223) を参照）。

## 要件

ノードは：
  - 資金調達トランザクションをブロードキャストした、または HTLC 出力を含むコミットメントトランザクションに対するコミットメント署名を送信した場合：
    - すべての出力が *取り返しのつかない形で解決された* 状態になるまで：
      - *取り返しのつかない形で解決* されていない出力を消費するトランザクションがないか、ブロックチェーンを監視しなければなりません。
  - 以下に指定される手順に従い、すべての出力を *解決* しなければなりません。
  - ブロックチェーンの再編成に備え、出力を複数回解決できるよう準備しておかなければなりません。
  - 資金調達トランザクションが消費され、かつチャネルがまだ閉じていない場合：
    - 説明的な `error` を送信してよいです。
    - チャネルを失敗させるべきです。
  - 無効なトランザクションは無視すべきです。

## 理論的根拠

ローカルノードが資金を預けている以上、リモートノードが一方的にクローズしないか確認するためにブロックチェーンを監視する必要があります。

無効なトランザクション（たとえば不正な署名のもの）は誰でも生成できますが、ブロックチェーン側で無視されるため、なんらかのアクションを引き起こすべきではありません。

# コミットメントトランザクション

ローカルノードとリモートノードはそれぞれ *コミットメントトランザクション* を保持しています。これらのコミットメントトランザクションには最大で 6 種類の出力があります：

1. _ローカルノードのメイン出力_：*ローカルノード* の delayed_pubkey に支払う出力で、0 個または 1 個。
2. _リモートノードのメイン出力_：*リモートノード* の delayed_pubkey に支払う出力で、0 個または 1 個。
3. _ローカルノードのアンカー出力_：*ローカルノード* の funding_pubkey に支払う出力で、1 個。
4. _リモートノードのアンカー出力_：*リモートノード* の funding_pubkey に支払う出力で、1 個。
5. _ローカルノードが提供する HTLC_：0 個以上の保留中の支払い (*HTLC*) で、支払いプレイメージと引き換えに *リモートノード* に支払います。
6. _リモートノードが提供する HTLC_：0 個以上の保留中の支払い (*HTLC*) で、支払いプレイメージと引き換えに *ローカルノード* に支払います。

ローカルノードとリモートノードに協力するインセンティブを与えるため、`OP_CHECKSEQUENCEVERIFY` による相対タイムアウトが、*ローカルノードの出力*（*ローカルノードのコミットメントトランザクション* 内）と *リモートノードの出力*（*リモートノードのコミットメントトランザクション* 内）に課されます。たとえばローカルノードが自分のコミットメントトランザクションを公開した場合、自分の資金を取り戻すには待たなければならない一方、リモートノードは自分の資金にすぐアクセスできます。その結果、2 つのコミットメントトランザクションは同一ではないものの、（通常は）対称的な構造になります。

詳細は [BOLT #3: Commitment Transaction](03-transactions.md#commitment-transaction) を参照してください。

# チャネルの失敗

チャネルを閉じる方法は複数ありますが、最も効率的な方法が望まれます。

さまざまなエラーケースがチャネルのクローズを伴います。ピアにエラーメッセージを送る際の要件は [BOLT #1: The `error` Message](01-messaging.md#the-error-message) に記載されています。

## 要件

ノードは：
  - *ローカルコミットメントトランザクション* がこれまで一度も `to_local` または HTLC 出力を含んでいない場合：
    - チャネルを単に忘れてよいです。
  - そうでない場合：
    - *現在のコミットメントトランザクション* に `to_local` や他の HTLC 出力が含まれていない場合：
      - リモートノードがチャネルを閉じるのを待つだけでよいです。
      - リモートノードが閉じるまで：
        - チャネルを忘れてはなりません。
    - そうでない場合：
      - 十分な手数料を含む有効な `closing_signed` メッセージを受け取った場合：
        - この手数料を使って *相互クローズ* を行うべきです。
      - そうでない場合：
      	- ノードがチャネルの状態が古いと知っている、もしくはそう見なしている場合：
      	  - *最後のコミットメントトランザクション* をブロードキャストしてはなりません。
        - そうでない場合：
          - 署名を持っている *最後のコミットメントトランザクション* をブロードキャストして *一方的クローズ* を行わなければなりません。
          - `to_local_anchor` 出力を消費する際に、コミットメントトランザクションをブロックに取り込ませるインセンティブとして十分な手数料を提供しなければなりません。第三者へ支払う場合は特に注意が必要です。これは、非アンカー出力に CSV 遅延を追加することで対処された脆弱性を再導入することになるためです。
          - 消費トランザクションがブロックに迅速に取り込まれるのに十分でないと判明した場合は、[replace-by-fee](https://github.com/bitcoin/bips/blob/master/bip-0125.mediawiki) などの仕組みを使うべきです。

## 理論的根拠

`dust_limit_satoshis` は非経済的な出力（さもなければブロックチェーン上に未使用のまま永久に残ってしまうもの）の生成を防ぐためのもので、コミットメントトランザクションのすべての出力は消費されなければなりません。

チャネルの初期段階では、片方がチャネル内にほとんど資金を持たない、あるいはまったく持たないことがよくあります。この場合は預けている資金がないので、ノードはチャネル状態の監視にリソースを割く必要はありません。

相互クローズは一方的クローズよりも好まれます。相互クローズの出力は遅延に縛られず、ウォレットから直接使えるためです。さらに、相互クローズの手数料はコミットメントトランザクションの手数料ほど過大になりにくい傾向もあります（`option_anchors` の場合は、コミットメントトランザクションをマイニングしてもらうために子トランザクションが必要になることもあります）。したがって、`closing_signed` の署名を使わない理由は、提示された手数料が処理されるには小さすぎる場合に限られます。

# 相互クローズの処理

クローズトランザクションは資金調達トランザクションの出力を *解決* します。

相互クローズの場合、合意済みの出力が指定された `scriptpubkey` に送られるため、ノードは特に他のことを行う必要はありません（[BOLT #2: クローズ開始: `shutdown`](02-peer-protocol.md#closing-initiation-shutdown) を参照）。

# 一方的クローズの処理：ローカルコミットメントトランザクション

これは一方的クローズに関する 2 つのケースのうちの 1 つ目です。この場合、ノードは自分の *ローカルコミットメントトランザクション* を発見し、これが資金調達トランザクションの出力を *解決* します。

ただし、自分が一方的クローズを開始したノードは、`OP_CHECKSEQUENCEVERIFY` の遅延（リモートノードの `to_self_delay` フィールドで指定）が経過するまで、自分の出力から資金を回収することができません。関係する箇所では以下に注記しています。

## 要件

ノードは：
  - *ローカルコミットメントトランザクション* を発見した場合：
    - `to_local` 出力を都合のよいアドレスに送って消費すべきです。
    - 出力を消費する前に、`OP_CHECKSEQUENCEVERIFY` の遅延（リモートノードの `to_self_delay` フィールドで指定）が経過するまで待たなければなりません。
      - 注：出力を（推奨どおり）消費した場合、その出力は消費トランザクションによって *解決* されます。そうでない場合は、コミットメントトランザクション自体によって *解決* されたと見なされます。
    - `to_remote` 出力は無視してよいです。
      - 注：ローカルノード側で行うべきことはなく、`to_remote` はコミットメントトランザクション自体によって *解決* されたと見なされます。
    - 自分自身が提供した HTLC は、[HTLC 出力の処理：ローカルコミットメント、ローカルオファー](#htlc-output-handling-local-commitment-local-offers) に従って処理しなければなりません。
    - リモートノードが提供した HTLC は、[HTLC 出力の処理：ローカルコミットメント、リモートオファー](#htlc-output-handling-local-commitment-remote-offers) に従って処理しなければなりません。

## 理論的根拠

`to_local` 出力をいったん消費しておけば、そのチャネルに固有の複雑な witness スクリプトを後々まで覚えておく必要がなくなります。

`to_remote` 出力はもっぱらリモートノードの問題なので、無視できます。

## HTLC 出力の処理：ローカルコミットメント、ローカルオファー

各 HTLC 出力は、タイムアウト後に HTLC-timeout トランザクションを使う *ローカル提供者*、または支払いプレイメージを持っている *リモート受取人* のいずれかしか消費できません。

HTLC は出力として表現されない場合もあります。ダストとしてトリミングされたか、トランザクションへのコミットが部分的にしか進んでいない場合などです。

HTLC 出力は、最新ブロックの高さが HTLC `cltv_expiry` 以上になった時点で *タイムアウト* します。

### 要件

ノードは：
  - コミットメントトランザクションの HTLC 出力が支払いプレイメージで消費された場合、その出力は *取り返しのつかない形で解決された* と見なされます：
    - トランザクション入力の witness から支払いプレイメージを抽出しなければなりません。
  - コミットメントトランザクションの HTLC 出力が *タイムアウト* し、まだ *解決* されていない場合：
    - HTLC-timeout トランザクションでその出力を消費して *解決* しなければなりません。
    - 解決トランザクションが妥当な深さに達したら：
      - 対応する受信 HTLC（あれば）を失敗させなければなりません。
      - その HTLC-timeout トランザクションの出力を解決しなければなりません。
      - HTLC-timeout トランザクションは、都合のよいアドレスに送って消費することで解決すべきです。
        - 注：出力を（推奨どおり）消費した場合、その出力は消費トランザクションによって *解決* されます。そうでない場合は、HTLC-timeout トランザクション自体によって *解決* されたと見なされます。
      - その HTLC-timeout 出力を消費する前に、`OP_CHECKSEQUENCEVERIFY` の遅延（リモートノードの `open_channel` の `to_self_delay` フィールドで指定）が経過するまで待たなければなりません。
  - このコミットメントトランザクションに対応する出力を持たない、コミット済みの HTLC については：
    - 支払いプレイメージが既知の場合：
      - 対応する受信 HTLC（あれば）を fulfill しなければなりません。
    - そうでない場合：
      - コミットメントトランザクションが妥当な深さに達したら：
        - 対応する受信 HTLC（あれば）を失敗させなければなりません。
      - その HTLC に対応する出力を含む *有効な* コミットメントトランザクションがどこにも存在しない場合：
        - 対応する受信 HTLC をより早く失敗させてよいです。

### 理論的根拠

支払いプレイメージは、（提供ノードが支払いの発信者である場合は）支払いの証明として、または（提供ノードが支払いを転送している場合は）別のピアからの対応する受信 HTLC を引き換えるために使われます。ノードがいったん支払いを抽出してしまえば、HTLC を消費するトランザクション自体の運命を気にする必要はなくなります。

両方の解決が同時に成立しうる場合（たとえばノードがタイムアウト後に支払いの成功を受け取った場合）、どちらの解釈も受け入れられます。受取人は、こうした事態が起きる前にその出力を消費する責任を負います。

ローカル HTLC-timeout トランザクションは、リモートノードが HTLC を fulfill して資金を請求してしまうのを防ぐために HTLC をタイムアウトさせる用途で使う必要があります。これを行ったうえで、ローカルノードは対応する受信 HTLC に対して `update_fail_htlc`（おそらく理由は `permanent_channel_failure`）でバックフェイル処理を行えます。詳細は [BOLT #2](02-peer-protocol.md#forwarding-htlcs) を参照してください。受信 HTLC もオンチェーン上にある場合、ノードは単にそれがタイムアウトするのを待つしかありません。早期失敗を通知する手段はないからです。

コミット済みの HTLC が確認済みコミットメントトランザクションに出力として現れない理由はいくつか考えられます。HTLC が `dust_limit_satoshis` より小さい、まだコミットメントトランザクションに追加されていない、すでに失敗または fulfill されている、などです。いずれの場合でも、HTLC の支払いプレイメージが既知であれば、資金を失わないように上流の HTLC を fulfill する必要があります。

支払いプレイメージが未知の場合に取るべき行動は、HTLC を含むコミットメントトランザクションへ確認済みのコミットメントトランザクションが入れ替わるようなブロックチェーン再編成の可能性に依存します。HTLC が *いかなるコミットメントトランザクション* にも収まらないほど小さい場合、そのような再編成は起こりえないため、即時に安全に失敗させられます。そうでない場合は、受信 HTLC を失敗させる前に再編成のための遅延を取る必要があります。受信 HTLC を自身のタイムアウトよりも前に失敗させなければならないという要件は、上限として依然有効です。

## HTLC 出力の処理：ローカルコミットメント、リモートオファー

各 HTLC 出力は、HTLC-success トランザクションを使う受取人だけが消費できます。受取人は支払いプレイメージを持っている場合に限ってこのトランザクションを完成させられます。プレイメージを持っていない（そして見つけられない）場合、HTLC 出力がタイムアウトしたあとに消費するのは提供者の責任です。

提供された HTLC についてはいくつかのパターンがあります：

1. 提供者がまだ取り返しのつかない形でコミットしていない。受取人は通常プレイメージを知りません。完全にコミットされるまで HTLC を転送しないからです。プレイメージを使うとこの受取人が最終ホップであることが露見してしまうため、HTLC をタイムアウトさせるのが最善です。
2. 提供者は提供 HTLC に対し取り返しのつかない形でコミットしているが、受取人はまだ送信側 HTLC にコミットしていない。この場合、受取人は提供 HTLC を転送するか、タイムアウトさせるかを選べます。
3. 受取人が、提供 HTLC と引き換えに送信側 HTLC にコミットしている。この場合、受取人は送信側 HTLC からプレイメージを受け取ったらそれを必ず使用しなければなりません。そうしないと、受信支払いを引き換えずに送信支払いだけを送ることになり、資金を失います。

### 要件

ローカルノードは：
  - 提供されたが未解決の HTLC 出力について、自分が送信側 HTLC にコミットしており、かつ支払いプレイメージを受け取った（または既に持っている）場合：
    - HTLC-success トランザクションでその出力を消費し、*解決* しなければなりません。
    - 自分が最終受取人でない場合は、自分のプレイメージを公開してはなりません。<sup>[Preimage-Extraction](https://lists.linuxfoundation.org/pipermail/lightning-dev/2020-October/002857.html)</sup>
    - その HTLC-success トランザクションの出力を解決しなければなりません。
  - そうでない場合：
    - *リモートノード* が HTLC に対し取り返しのつかない形でコミットしていない場合：
      - その出力を消費して *解決* してはなりません。
  - その HTLC-success トランザクションの出力は、都合のよいアドレスに送って消費することで解決すべきです。
  - その HTLC-success トランザクションの出力を消費する前に、`OP_CHECKSEQUENCEVERIFY` の遅延（*リモートノード* の `open_channel` の `to_self_delay` フィールドで指定）が経過するまで待たなければなりません。

出力を（推奨どおり）消費した場合、その出力は消費トランザクションによって *解決* されます。そうでない場合は、HTLC-success トランザクション自体によって *解決* されたと見なされます。

それ以外の方法で解決されない場合、HTLC 出力が期限切れになった時点で *取り返しのつかない形で解決された* と見なされます。

# 一方的クローズの処理：リモートコミットメントトランザクション

*リモートノード* のコミットメントトランザクションは、資金調達トランザクションの出力を *解決* します。

このケースではノードの動作を制約する遅延がないため、ローカルコミットメントトランザクションを発見したケース（[一方的クローズの処理：ローカルコミットメントトランザクション](#unilateral-close-handling-local-commitment-transaction) 参照）よりも扱いやすくなります。

## 要件

ローカルノードは：
  - *リモートノード* がブロードキャストした *有効な* コミットメントトランザクションを発見した場合：
    - 可能であれば：
      - 各出力を以下の指定どおりに処理しなければなりません。
      - 関連する `to_remote` については何もしなくてよいです。これは *ローカルノード* への単純な P2WPKH 出力です。
        - 注：`to_remote` はコミットメントトランザクション自体によって *解決* されたと見なされます。
      - 関連する `to_local` については何もしなくてよいです。これは *リモートノード* への支払い出力です。
        - 注：`to_local` はコミットメントトランザクション自体によって *解決* されたと見なされます。
      - 自分自身が提供した HTLC は、[HTLC 出力の処理：リモートコミットメント、ローカルオファー](#htlc-output-handling-remote-commitment-local-offers) に従って処理しなければなりません。
      - リモートノードが提供した HTLC は、[HTLC 出力の処理：リモートコミットメント、リモートオファー](#htlc-output-handling-remote-commitment-remote-offers) に従って処理しなければなりません。
    - そうでない場合（何らかの理由でブロードキャストを処理できない場合）：
      - 失われる可能性のある資金についてユーザに通知しなければなりません。

## 理論的根拠

`commitment_signed` で署名を受け取った後、対応する `revoke_and_ack` までの間には、有効な *未取り消し* のコミットメントトランザクションが複数存在しえます。そのため、どちらのコミットメントも *リモートノード* のコミットメントトランザクションとして機能しうるので、ローカルノードは両方を処理できる必要があります。

データ損失が起きた場合、ローカルノードは *リモートノード* のコミットメントトランザクションが持つ HTLC 出力をすべて認識できない状態に陥ることがあります。この状態は、署名済みであるにもかかわらずコミットメント番号が予想より大きいことから検出可能です。ノードはトランザクション用の自分の `remotepubkey` を導出して自分の資金を救出できます。注：このシナリオでは HTLC を救出することはできません。

## HTLC 出力の処理：リモートコミットメント、ローカルオファー

各 HTLC 出力は、タイムアウト後の *ローカル提供者*、または支払いプレイメージを持っているときに HTLC-success トランザクションを使う *リモート受取人* のいずれかしか消費できません。

HTLC が出力として表現されないこともあります。ダストとしてトリミングされた場合や、リモートノードが互いに異なる HTLC を含む 2 つの *有効な* コミットメントトランザクションを保持している場合などです。

HTLC 出力は、最新ブロックの深さが HTLC `cltv_expiry` 以上になった時点で *タイムアウト* します。

### 要件

ローカルノードは：
  - コミットメントトランザクションの HTLC 出力が支払いプレイメージで消費された場合：
    - HTLC-success トランザクションの入力 witness から支払いプレイメージを抽出しなければなりません。
      - 注：出力は *取り返しのつかない形で解決された* と見なされます。
  - コミットメントトランザクションの HTLC 出力が *タイムアウト* し、まだ *解決* されていない場合：
    - その出力を都合のよいアドレスに送って *解決* しなければなりません。
  - このコミットメントトランザクションに対応する出力を持たない、コミット済みの HTLC については：
    - 支払いプレイメージが既知の場合：
      - 対応する受信 HTLC（あれば）を fulfill しなければなりません。
    - そうでない場合：
      - コミットメントトランザクションが妥当な深さに達したら：
        - 対応する受信 HTLC（あれば）を失敗させなければなりません。
      - その HTLC に対応する出力を含む *有効な* コミットメントトランザクションがどこにも存在しない場合：
        - 対応する受信 HTLC をより早く失敗させてよいです。

### 理論的根拠

コミットメントトランザクションが *リモート* ノードに属する場合、（支払いプレイメージを使って）HTLC 出力を消費する唯一の方法は、HTLC-success トランザクションを使うことです。

支払いプレイメージは、（提供ノードが支払いの発信者である場合は）支払いの証明として、または（提供ノードが支払いを転送している場合は）別のピアからの対応する受信 HTLC を引き換えるために使われます。ノードがいったん支払いを抽出してしまえば、HTLC を消費するトランザクション自体の運命を気にする必要はなくなります。

両方の解決が同時に成立しうる場合（たとえばノードがタイムアウト後に支払いの成功を受け取った場合）、どちらの解釈も受け入れられます。受取人は、こうした事態が起きる前にその出力を消費する責任を負います。

タイムアウト後、ローカルノードは（リモートノードが HTLC-success トランザクションを使うのを防ぐため）HTLC 出力を消費する必要があり、これを行ったうえで対応する受信 HTLC に対して `update_fail_htlc`（おそらく理由は `permanent_channel_failure`）でバックフェイル処理を行えます。詳細は [BOLT #2](02-peer-protocol.md#forwarding-htlcs) を参照してください。受信 HTLC もオンチェーン上にある場合、ノードは単にそれがタイムアウトするのを待ちます。早期失敗を通知する手段はないからです。

コミット済みの HTLC が確認済みコミットメントトランザクションに出力として現れない理由はいくつか考えられます。HTLC が `dust_limit_satoshis` より小さい、まだコミットメントトランザクションに追加されていない、すでに失敗または fulfill されている、などです。いずれの場合でも、HTLC の支払いプレイメージが既知であれば、資金を失わないように上流の HTLC を fulfill する必要があります。

支払いプレイメージが未知の場合に取るべき行動は、HTLC を含むコミットメントトランザクションへ確認済みのコミットメントトランザクションが入れ替わるようなブロックチェーン再編成の可能性に依存します。HTLC が *いかなるコミットメントトランザクション* にも収まらないほど小さい場合、そのような再編成は起こりえないため、即時に安全に失敗させられます。そうでない場合は、受信 HTLC を失敗させる前に再編成のための遅延を取る必要があります。受信 HTLC を自身のタイムアウトよりも前に失敗させなければならないという要件は、上限として依然有効です。

## HTLC 出力の処理：リモートコミットメント、リモートオファー

リモート HTLC 出力は、ローカルノードが支払いプレイメージを持っている場合にのみ消費できます。ローカルノードがプレイメージを持っていない（そして見つけられない）場合、HTLC 出力がタイムアウトしたあとに消費するのはリモートノードの責任です。

提供された HTLC については、実際にはいくつかのパターンがあります：

1. 提供者がまだ取り返しのつかない形でコミットしていない。この場合、受取人は通常プレイメージを知りません。完全にコミットされるまで HTLC を転送しないからです。プレイメージを使うとこの受取人が最終ホップであることが露見してしまうため、HTLC をタイムアウトさせるのが最善です。
2. 提供者は提供 HTLC に対し取り返しのつかない形でコミットしているが、受取人はまだ送信側 HTLC にコミットしていない。この場合、受取人は HTLC を転送するか、タイムアウトを待つかを選べます。
3. 受取人が、提供 HTLC と引き換えに送信側 HTLC にコミットしている。この場合、受取人は送信側 HTLC からプレイメージを受け取ったらそれを必ず使用しなければなりません。そうしないと、受信支払いを引き換えずに送信支払いだけを送ることになり、資金を失います。

### 要件

ローカルノードは：
  - 提供されたが未解決の HTLC 出力について、自分が送信側 HTLC にコミットしており、かつ支払いプレイメージを受け取った（または既に持っている）場合：
    - その出力を都合のよいアドレスに送って消費し、*解決* しなければなりません。
  - そうでない場合：
    - リモートノードが HTLC に対し取り返しのつかない形でコミットしていない場合：
      - その出力を消費して *解決* してはなりません。

それ以外の方法で解決されない場合、HTLC 出力が期限切れになった時点で *取り返しのつかない形で解決された* と見なされます。

# 取り消されたトランザクションクローズの処理

いずれかのノードが古いコミットメントトランザクション（最新以外の以前のコミットメントトランザクション）をブロードキャストして不正を試みた場合、チャネルのもう一方のノードは取り消し用の秘密鍵を使い、チャネルの元の資金調達トランザクションからすべての資金を請求できます。

## 要件

ノードが、*自分自身* が取り消し用秘密鍵を持つコミットメントトランザクションを発見した時点で、資金調達トランザクションの出力は *解決* されます。

ローカルノードは：
  - *自分自身* がすでに `per_commitment_secret` を公開してしまったコミットメントトランザクションをブロードキャストしてはなりません。
  - _ローカルノードのメイン出力_ については何もしなくてよいです。これは自分宛ての単純な P2WPKH 出力です。
    - 注：この出力はコミットメントトランザクション自体によって *解決* されたと見なされます。
  - 取り消し用秘密鍵を使い、_リモートノードのメイン出力_ を *解決* しなければなりません。
  - _リモートノードが提供した HTLC_ を、次の 3 つの方法のいずれかで *解決* しなければなりません：
    * 支払いの取り消し用秘密鍵を使って *コミットメント tx* を消費する。
    * 支払いプレイメージを使って *コミットメント tx* を消費する（既知の場合）。
    * リモートノードが公開していれば、*HTLC-timeout tx* を消費する。
  - _ローカルノードが提供した HTLC_ を、次の 3 つの方法のいずれかで *解決* しなければなりません：
    * 支払いの取り消し用秘密鍵を使って *コミットメント tx* を消費する。
    * HTLC のタイムアウトが過ぎた後に *コミットメント tx* を消費する。
    * リモートノードが公開していれば、*HTLC-success tx* を消費する。
  - 取り消し用秘密鍵を使い、_リモートノードの HTLC-timeout トランザクション_ を *解決* しなければなりません。
  - 取り消し用秘密鍵を使い、_リモートノードの HTLC-success トランザクション_ を *解決* しなければなりません。
  - 支払いプレイメージがまだ既知でない場合、トランザクション入力の witness からそれを抽出すべきです。
  - `option_anchors` が適用される場合：
    - 単一のトランザクションですべての出力を *解決* してよいです。
    - 期限まで残り `security_delay` ブロックに達するまでに確認が行われない場合：
      - 取り消された各出力をそれぞれ独立したペナルティトランザクションで *解決* すべきです。複数の取り消された出力を一度に請求する単一のペナルティトランザクションは、トランザクションピンニング攻撃により確認をブロックされる可能性があります。
  - そうでない場合：
    - 単一のトランザクションですべての出力を *解決* してよいです。
  - 自身のトランザクションが HTLC トランザクションによって無効化される事態にも対応しなければなりません。

## 理論的根拠

すべての出力を解決する単一のトランザクションは、当事者ごと 483 HTLC の制限（[BOLT #2](02-peer-protocol.md#the-open_channel-message) を参照）により、標準サイズ制限の範囲に収まります。

注：`option_anchors` が適用される場合、不正を働くノードは SIGHASH_SINGLE の可鍛性を利用して、自分の HTLC-timeout / HTLC-success 出力の消費をピンニングできます。このため、すべての取り消された出力に対して単一のペナルティトランザクションを使うのは安全ではありません。_ローカルノードの `to_local` 出力_ の相対ロックタイムが満了するまで伝播を妨げられ、不正者がこの出力に対するペナルティを免れる恐れがあるためです。ただし、ピンニングトランザクションが確認された場合でも、第二レベルの取り消された出力に対する正当な処罰は妨げられません。

`security_delay` は、取り消された出力の絶対期限に対する固定点であり、処罰側のノードはこの時点までに、取り消された出力に対する単一消費トランザクションをブロードキャストし、確認されるまで積極的に手数料を引き上げ続けなければなりません。`security_delay` の正確な値はノードのポリシーに委ねられますが、18 ブロック（受信 HTLC の期限と同様）を推奨します。

## ペナルティトランザクションの重量計算

ペナルティトランザクションには 3 種類のスクリプトがあり、それぞれの witness の重量は次のとおりです（重量計算の詳細は [付録 A](#appendix-a-expected-weights) を参照）：

    to_local_penalty_witness: 160 bytes
    offered_htlc_penalty_witness: 243 bytes
    accepted_htlc_penalty_witness: 249 bytes

ペナルティ *txinput* 自体は 41 バイトを占め、重量は 164 バイトです。これにより、各入力の重量は次のようになります：

    to_local_penalty_input_weight: 324 bytes
    offered_htlc_penalty_input_weight: 407 bytes
    accepted_htlc_penalty_input_weight: 413 bytes

ペナルティトランザクションの残りの部分は、4+1+1+8+1+34+4=53 バイトの非 witness データを占めます。これは、（最大の標準出力スクリプトである）pay-to-witness-script-hash の出力を持ち、加えて 2 バイトの witness ヘッダがある前提です。

これらの出力を消費することに加え、ペナルティトランザクションは（手数料として支払う総額を減らすなどの目的で）コミットメントトランザクションの `to_remote` 出力をオプションで消費してもかまいません。これを行うには P2WPKH の witness と追加の *txinput* が必要となり、108 + 164 = 272 バイト分が増えます。

最悪のケースでは、ノードは受信 HTLC のみを保持し、HTLC-timeout トランザクションも公開されておらず、コミットメントトランザクションから消費せざるを得ません。

標準の最大重量を 400000 バイトとすると、単一のトランザクションでスイープできる HTLC の最大数は次のように計算できます：

    max_num_htlcs = (400000 - 324 - 272 - (4 * 53) - 2) / 413 = 966

したがって、（`to_local` と `to_remote` 出力を含む）483 個の双方向 HTLC を単一のペナルティトランザクションで解決できます。注：`to_remote` 出力をスイープしない場合でも、`max_num_htlcs` は 967 となり、片方向の上限である 483 HTLC は同じままです。

# HTLC トランザクションの生成

`option_anchors` がコミットメントトランザクションに適用されない場合、HTLC-timeout および HTLC-success トランザクションは（うまくいけば）妥当な手数料を持つ完全なトランザクションであり、そのまま直接使う必要があります。

そうでない場合、ピアから受け取った HTLC 署名には `SIGHASH_SINGLE|SIGHASH_ANYONECANPAY` を使わなければなりません。これにより、HTLC トランザクションを他のトランザクションと組み合わせられるようになります。ローカル署名は `SIGHASH_ALL` を使わなければなりません。そうしないと、誰でも追加の入力や出力をトランザクションに付け加えられてしまいます。

`option_anchors` が適用される場合、HTLC-timeout および HTLC-success トランザクションは入力と出力が同じ値になるよう署名されます。これは手数料がゼロであることを意味し、妥当な手数料に到達させるために他の入力と組み合わせなければなりません。

## 要件

コミットメントトランザクションに対応する HTLC-success または HTLC-timeout トランザクションをブロードキャストするノードは：
  - `option_anchors` が適用される場合：
    - ブロックに迅速に取り込まれるのに十分な手数料を寄与する入力と組み合わせなければなりません。
    - 他のトランザクションと組み合わせてよいです。

# 一般的な要件

ノードは：
  - 資金調達トランザクションの出力を消費するトランザクションを発見し、それが上記のいずれのカテゴリ（相互クローズ、一方的クローズ、取り消されたトランザクションクローズ）にも該当しない場合：
    - 失われる可能性のある資金についてユーザに警告しなければなりません。
      - 注：そのような不審なトランザクションが存在するということは、秘密鍵が漏洩しており、結果として資金が失われている可能性を示唆します。
  - トランザクションの監視は、最も作業量の多いチェーンの内容のみを対象としてよいです。
    - 注：オンチェーン HTLC は十分にまれであるべきで、速度を最優先する必要はありません。
  - （有効な）ブロードキャスト済みトランザクション、いわゆるメンプールを監視してよいです。
    - 注：メンプール監視を行うと、HTLC の引き出しの待ち時間がより短くなるはずです。

# 付録 A: 期待される重量

## `to_local` ペナルティトランザクションの witness の期待される重量

[BOLT #3](03-transactions.md) で説明されているとおり、このトランザクションの witness は次のとおりです：

    <sig> 1 { OP_IF <revocationpubkey> OP_ELSE to_self_delay OP_CSV OP_DROP <local_delayedpubkey> OP_ENDIF OP_CHECKSIG }

`to_local` ペナルティトランザクションの witness の *期待される重量* は次のように計算されます：

    to_local_script: 83 bytes
        - OP_IF: 1 byte
            - OP_DATA: 1 byte (revocationpubkey length)
            - revocationpubkey: 33 bytes
        - OP_ELSE: 1 byte
            - OP_DATA: 1 byte (delay length)
            - delay: 8 bytes
            - OP_CHECKSEQUENCEVERIFY: 1 byte
            - OP_DROP: 1 byte
            - OP_DATA: 1 byte (local_delayedpubkey length)
            - local_delayedpubkey: 33 bytes
        - OP_ENDIF: 1 byte
        - OP_CHECKSIG: 1 byte

    to_local_penalty_witness: 160 bytes
        - number_of_witness_elements: 1 byte
        - revocation_sig_length: 1 byte
        - revocation_sig: 73 bytes
        - one_length: 1 byte
        - witness_script_length: 1 byte
        - witness_script (to_local_script)

## `offered_htlc` ペナルティトランザクションの witness の期待される重量

`offered_htlc` ペナルティトランザクションの witness の *期待される重量* は次のように計算されます（一部の計算は [BOLT #3](03-transactions.md) で既に行われています）：

    offered_htlc_script: 133 bytes

    offered_htlc_penalty_witness: 243 bytes
        - number_of_witness_elements: 1 byte
        - revocation_sig_length: 1 byte
        - revocation_sig: 73 bytes
        - revocation_key_length: 1 byte
        - revocation_key: 33 bytes
        - witness_script_length: 1 byte
        - witness_script (offered_htlc_script)

## `accepted_htlc` ペナルティトランザクションの witness の期待される重量

`accepted_htlc` ペナルティトランザクションの witness の *期待される重量* は次のように計算されます（一部の計算は [BOLT #3](03-transactions.md) で既に行われています）：

    accepted_htlc_script: 139 bytes


    accepted_htlc_penalty_witness: 249 bytes
        - number_of_witness_elements: 1 byte
        - revocation_sig_length: 1 byte
        - revocation_sig: 73 bytes
        - revocationpubkey_length: 1 byte
        - revocationpubkey: 33 bytes
        - witness_script_length: 1 byte
        - witness_script (accepted_htlc_script)

# 著者

[FIXME:]

![Creative Commons License](https://i.creativecommons.org/l/by/4.0/88x31.png "License CC-BY")
<br>
この作品は [Creative Commons Attribution 4.0 International License](http://creativecommons.org/licenses/by/4.0/) の下でライセンスされています。
