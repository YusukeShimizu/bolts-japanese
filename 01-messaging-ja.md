# BOLT #1: 基本プロトコル

## 概要

このプロトコルは、個々のメッセージのフレーミングを担う、認証済みかつ順序付けられたトランスポート機構が存在することを前提としています。Lightning で使用する標準的なトランスポート層は [BOLT #8](08-transport.md) で規定されていますが、上記の保証を満たすトランスポートであれば置き換え可能です。

デフォルトの TCP ポートは使用するネットワークによって異なります。代表的なネットワークは次のとおりです:

- Bitcoin メインネット: ポート番号 9735 (16 進数表記では `0x2607`)
- Bitcoin テストネット: ポート番号 19735 (`0x4D17`)
- Bitcoin signet: ポート番号 39735 (`0x9B37`)

LIGHTNING の Unicode コードポイント<sup>[1](#reference-1)</sup>と、このポート割り当ての慣例はいずれも Bitcoin Core の慣例に従っています。

特に指定がない限り、すべてのデータフィールドは符号なしビッグエンディアンです。

## 目次

  * [接続処理と多重化](#connection-handling-and-multiplexing)
  * [Lightning メッセージフォーマット](#lightning-message-format)
  * [タイプ・長さ・値フォーマット](#type-length-value-format)
  * [基本的な型](#fundamental-types)
  * [セットアップメッセージ](#setup-messages)
    * [`init` メッセージ](#the-init-message)
    * [`error` と `warning` メッセージ](#the-error-and-warning-messages)
  * [制御メッセージ](#control-messages)
    * [`ping` と `pong` メッセージ](#the-ping-and-pong-messages)
  * [ピアストレージ](#peer-storage)
    * [`peer_storage` と `peer_storage_retrieval` メッセージ](#the-peer_storage-and-peer_storage_retrieval-messages)
  * [付録 A: BigSize テストベクトル](#appendix-a-bigsize-test-vectors)
  * [付録 B: タイプ・長さ・値テストベクトル](#appendix-b-type-length-value-test-vectors)
  * [付録 C: メッセージ拡張](#appendix-c-message-extension)
  * [付録 D: 符号付き整数テストベクトル](#appendix-d-signed-integers-test-vectors)
  * [謝辞](#acknowledgments)
  * [参考文献](#references)
  * [著者](#authors)

## 接続処理と多重化

実装はピアごとに単一の接続を使用しなければなりません。チャネルメッセージ (チャネル ID を含む) は、この単一の接続上で多重化されます。

## Lightning メッセージフォーマット

復号後、すべての Lightning メッセージは次の形式になります:

1. `type`: メッセージの種類を示す 2 バイトのビッグエンディアンフィールド
2. `payload`: メッセージの残りを構成する可変長のペイロード。`type` に対応する形式に従います
3. `extension`: オプションの [TLV ストリーム](#type-length-value-format)

`type` フィールドは `payload` フィールドの解釈方法を示します。各 type のフォーマットは、このリポジトリ内の仕様で定義されています。type は _it's ok to be odd_ ルールに従い、ノードは受信者がそれを理解しているか確認することなく _奇数_ 番号の type を送信してよいことになっています。

メッセージは論理的に 5 つのグループに分類され、設定された最上位ビットの位置によって順序付けられています:

  - セットアップ & コントロール (type `0`-`31`): 接続セットアップ、制御、サポート機能、エラー報告に関するメッセージ (本文書で説明)
  - チャネル (type `32`-`127`): マイクロペイメントチャネルの開設と解体に用いるメッセージ ([BOLT #2](02-peer-protocol.md) で説明)
  - コミットメント (type `128`-`255`): 現在のコミットメントトランザクションの更新に関するメッセージ。HTLC の追加・取り消し・決済、手数料の更新、署名の交換を含む ([BOLT #2](02-peer-protocol.md) で説明)
  - ルーティング (type `256`-`511`): ノードとチャネルのアナウンスメント、およびアクティブなルート探索を含むメッセージ ([BOLT #7](07-routing-gossip.md) で説明)
  - カスタム (type `32768`-`65535`): 実験的およびアプリケーション固有のメッセージ

メッセージのサイズはトランスポート層により 2 バイトの符号なし整数に収まる必要があり、したがって最大サイズは 65535 バイトです。

送信ノードは:
  - 事前の合意なしに、ここに列挙されていない偶数 type のメッセージを送信してはなりません。
  - 事前の合意なしに、`extension` 内で偶数 type の TLV レコードを送信してはなりません。
  - この仕様内でオプションを交渉する場合:
    - そのオプションで注釈付けされたすべてのフィールドを含めなければなりません。
  - カスタムメッセージを定義する際:
    - 他のカスタム type との衝突を避けるためにランダムな `type` を選ぶべきです。
    - [この issue](https://github.com/lightningnetwork/lightning-rfc/issues/716) に列挙されている他の実験と衝突しない `type` を選ぶべきです。
    - 通常のノードが追加データを無視するのが望ましい場合は、奇数の `type` 識別子を選ぶべきです。
    - 通常のノードがメッセージを拒否し接続を閉じるのが望ましい場合は、偶数の `type` 識別子を選ぶべきです。

受信ノードは:
  - _奇数_ かつ未知の type のメッセージを受信したとき:
    - 受信したメッセージを無視しなければなりません。
  - _偶数_ かつ未知の type のメッセージを受信したとき:
    - 接続を閉じなければなりません。
    - チャネルを失敗させてよいです。
  - 内容に対して長さが不足している既知のメッセージを受信したとき:
    - 接続を閉じなければなりません。
    - チャネルを失敗させてよいです。
  - `extension` を含むメッセージを受信したとき:
    - `extension` を無視してよいです。
    - そうでない場合で、`extension` が無効なとき:
      - 接続を閉じなければなりません。
      - チャネルを失敗させてよいです。

### 設計上の根拠

デフォルトで `SHA2` と Bitcoin 公開鍵はいずれもビッグエンディアンでエンコードされるため、他のフィールドだけ別のエンディアンを使うのは不自然です。

長さは暗号ラッピングによって 65535 バイトに制限されており、本プロトコルのメッセージはそもそもこの長さを超えることはありません。

_it's ok to be odd_ ルールにより、クライアント側での交渉や特別な実装なしに、将来の任意拡張が可能になります。同様に _extension_ フィールドも、送信者に追加の TLV データを含めることを許すことで将来の拡張を可能にします。なお _extension_ フィールドは、メッセージの `payload` がすでに最大長 65535 バイトを満たしていない場合にのみ追加できる点に注意してください。

実装によっては、メッセージデータを 8 バイト境界 (ここで定義される型の中で最大の自然なアライメント要件) に揃えたい場合があるかもしれません。しかし、type フィールドの後に 6 バイトのパディングを入れるのは無駄と判断されました。アライメントは、メッセージを 6 バイトの先頭パディング付きバッファに復号することで実現できます。

## タイプ・長さ・値フォーマット

プロトコル全体を通じて、既存のメッセージ type に新しいフィールドを後方互換に追加できるよう、TLV (Type-Length-Value) フォーマットが用いられています。

`tlv_record` は 1 つのフィールドを表し、次の形式でエンコードされます:

* [`bigsize`: `type`]
* [`bigsize`: `length`]
* [`length`: `value`]

`tlv_stream` は (0 個の場合もある) `tlv_record` の連なりであり、エンコードされた `tlv_record` を連結して表現します。既存のメッセージを拡張する用途では、`tlv_stream` は通常、現在定義されているすべてのフィールドの後ろに配置されます。

`type` は BigSize フォーマットでエンコードされます。これはメッセージごとに固有な 64 ビット識別子として機能し、`value` の内容をどのようにデコードするかを決定します。2^16 未満の `type` 識別子は本仕様での使用のために予約されており、2^16 以上の `type` 識別子はカスタムレコード用に利用できます。本仕様で定義されていないレコードはすべてカスタムレコードと見なされ、実験的なものやアプリケーション固有のメッセージもこれに含まれます。

`length` は BigSize フォーマットでエンコードされ、`value` のサイズをバイト単位で示します。

`value` は完全に `type` に依存し、`type` によって定まるメッセージ固有のフォーマットに従ってエンコード/デコードされるべきです。

### 要件

送信ノードは:
 - `tlv_stream` 内の `tlv_record` を厳密に増加する `type` の順に並べなければなりません。したがって同一の `type` を持つ TLV レコードを複数生成してはなりません。
 - `type` と `length` を最小限のバイト数でエンコードしなければなりません。
 - カスタムレコードの `type` 識別子を定義する際:
   - 他のカスタム type との衝突を避けるためにランダムな `type` 識別子を選ぶべきです。
   - 通常のノードが追加データを無視するのが望ましい場合は、奇数の `type` 識別子を選ぶべきです。
   - 通常のノードがそのカスタムレコードを含む TLV ストリーム全体を拒否するのが望ましい場合は、偶数の `type` 識別子を選ぶべきです。
 - `tlv_record` 内で冗長な可変長エンコーディングを使用すべきではありません。

受信ノードは:
 - `type` を読み始める前に残りバイトが 0 の場合:
   - `tlv_stream` の解析を停止しなければなりません。
 - `type` または `length` が最小限にエンコードされていない場合:
   - `tlv_stream` の解析を失敗としなければなりません。
 - デコードした `type` が厳密に増加していない場合 (同じ `type` が 2 回以上出現する場合を含む):
   - `tlv_stream` の解析を失敗としなければなりません。
 - `length` がメッセージ内の残りバイト数を超える場合:
   - `tlv_stream` の解析を失敗としなければなりません。
 - `type` が既知の場合:
   - 既知のエンコーディングを用いて、続く `length` バイトをデコードしなければなりません。
   - `length` が、その `type` の既知のエンコーディングに必要な長さと完全に一致しない場合:
     - `tlv_stream` の解析を失敗としなければなりません。
   - その `type` の既知のエンコーディング内に含まれる可変長フィールドが最小限でない場合:
     - `tlv_stream` の解析を失敗としなければなりません。
 - そうでない場合、`type` が未知のとき:
   - `type` が偶数なら:
     - `tlv_stream` の解析を失敗としなければなりません。
   - そうでなく `type` が奇数なら:
     - 続く `length` バイトを破棄しなければなりません。

### 設計上の根拠

TLV を使う主な利点は、各フィールドがエンコードされた要素の正確なサイズを保持しているため、読み手が理解できない新しいフィールドを安全に無視できる点です。TLV がない場合、ノードが特定のフィールドを使う気がなくても、後続フィールドのオフセットを求めるためにそのフィールドの解析ロジックを実装せざるを得ません。

厳密な単調増加の制約により、すべての `type` が一意で、高々一度しか現れないことが保証されます。ベクタ、マップ、構造体などの複雑なオブジェクトに対応するフィールドは、オブジェクトが単一の `tlv_record` 内にシリアライズされるようにエンコードを定義すべきです。一意性の制約は、特に次のような最適化を可能にします:
 - 正規の順序がエンコードされた `value` に依らず定義できる。
 - 正規の順序をエンコード時に動的に決定するのではなく、コンパイル時に決定できる。
 - 正規の順序の検証に必要な状態が少なくて済み、コストも低くなる。
 - 可変サイズのフィールドが、要素を逐次追加してダブル&コピーのオーバーヘッドを発生させる代わりに、見込みサイズを事前に予約できる。

`type` と `length` に bigsize を使うことで、`type` が小さい場合や `value` が短い場合にスペースを節約できます。その結果、通信路上やオニオンペイロード内でアプリケーションデータに使えるスペースが増える可能性があります。

すべての `type` は、底にある `tlv_record` の正規エンコーディングを得るため、増加順に並んでいなければなりません。これは `tlv_stream` に対して署名を計算する際に重要であり、検証者が署名者と同じメッセージダイジェストを再計算できることを保証します。フィールド集合に対する正規の順序は、検証者がフィールドの中身を理解していなくても強制可能である点に注意してください。

書き手は `tlv_record` 内で冗長な可変長エンコーディングを使うことを避けるべきです。長さを二重にエンコードすることになり、外側の長さの計算も煩雑になるためです。たとえば可変長のバイト列を書く場合、`tlv_record` はすでに後続バイト数を持っているので、`value` には生のバイトだけを入れて内部の長さフィールドは省くべきです。一方で、`tlv_record` が複数の可変長要素を含む場合は、受信者が `value` から個々の要素を解析できるようにするために必要であり、冗長とは見なされません。

## 基本的な型

メッセージ仕様では、さまざまな基本的な型を参照します。

* `byte`: 8 ビットのバイト
* `s8`: 8 ビットの符号付き整数
* `u16`: 2 バイトの符号なし整数
* `s16`: 2 バイトの符号付き整数
* `u32`: 4 バイトの符号なし整数
* `s32`: 4 バイトの符号付き整数
* `u64`: 8 バイトの符号なし整数
* `s64`: 8 バイトの符号付き整数

符号付き整数は、標準的なビッグエンディアンの 2 の補数表現を使用します (テストベクタは[下記](#appendix-d-signed-integers-test-vectors)を参照)。

TLV レコードの末尾の値には、切り詰め整数 (truncated integer) を使用できます。切り詰め整数では、先頭のゼロを省略しなければなりません。

* `tu16`: 0 から 2 バイトの切り詰め符号なし整数
* `tu32`: 0 から 4 バイトの切り詰め符号なし整数
* `tu64`: 0 から 8 バイトの切り詰め符号なし整数

金額をエンコードする目的で使用する場合、これらのフィールドは 2100 万 BTC の上限を満たしていなければなりません。

* satoshi 単位の金額は最大で `0x000775f05a074000` でなければなりません
* milli-satoshi 単位の金額は最大で `0x1d24b2dfac520000` でなければなりません

便宜のため、以下の型も定義されています。

* `chain_hash`: 32 バイトのチェーン識別子 ([BOLT #0](00-introduction.md#glossary-and-terminology-guide) を参照)
* `channel_id`: 32 バイトの channel_id ([BOLT #2](02-peer-protocol.md#definition-of-channel-id) を参照)
* `sha256`: 32 バイトの SHA2-256 ハッシュ
* `signature`: 64 バイトの bitcoin 楕円曲線署名
* `bip340sig`: [BIP-340](https://github.com/bitcoin/bips/blob/master/bip-0340.mediawiki) に従う 64 バイトの bitcoin 楕円曲線 Schnorr 署名
* `point`: 33 バイトの楕円曲線上の点 ([SEC 1 standard](http://www.secg.org/sec1-v2.pdf#subsubsection.2.3.3) に従う圧縮エンコーディング)
* `short_channel_id`: チャネルを識別する 8 バイトの値 ([BOLT #7](07-routing-gossip.md#definition-of-short-channel-id) を参照)
* `sciddir_or_pubkey`: ノードを参照または識別する 9 バイトまたは 33 バイトのいずれか
    * 先頭バイトが 0 または 1 の場合、続いて 8 バイトの `short_channel_id` が並び、合計 9 バイトになります
        * 先頭バイトが 0 の場合、これは `short_channel_id` の `channel_announcement` における `node_id_1` を指します
        * 先頭バイトが 1 の場合、これは `short_channel_id` の `channel_announcement` における `node_id_2` を指します
          ([BOLT #7](07-routing-gossip.md#the-channel_announcement-message) を参照)
    * 先頭バイトが 2 または 3 の場合、値は 33 バイトの `point` です
* `bigsize`: bitcoin の CompactSize エンコーディングに似た可変長の符号なし整数。ただしビッグエンディアン。詳細は [BigSize](#appendix-a-bigsize-test-vectors) で説明されます。
* `utf8`: UTF-8 文字列を構成する 1 バイト。書き手はこの型の配列が有効な UTF-8 文字列であることを保証しなければなりません。読み手は、この型の配列が有効な UTF-8 文字列になっていないメッセージを拒否してよいです。

## セットアップメッセージ

### `init` メッセージ

認証が完了したあとの最初のメッセージで、そのノードがサポートまたは要求する機能を提示します。これは再接続時であっても同様です。

[BOLT #9](09-features.md) には機能の一覧が規定されています。各機能は通常 2 ビットで表現されます。最下位ビットは番号 0 で、これは _偶数_ です。次のビットは番号 1 で、これは _奇数_ です。歴史的経緯から、機能はグローバルとローカルの機能ビットマスクに分かれています。

ピアが現在の接続の `init` メッセージで (偶数か奇数のどちらかとして) ある機能を立てている場合、その機能は *提供されている (offered)* と言います。両ピアがその機能を提供している場合、またはローカルノードが偶数として提供している場合 (この場合ピアがサポートしていなければ切断するはずなので、サポートしていると仮定できる)、その機能は *交渉済み (negotiated)* と言います。

`features` フィールドは 0 でバイト境界までパディングしなければなりません。

1. type: 16 (`init`)
2. data:
   * [`u16`:`gflen`]
   * [`gflen*byte`:`globalfeatures`]
   * [`u16`:`flen`]
   * [`flen*byte`:`features`]
   * [`init_tlvs`:`tlvs`]

1. `tlv_stream`: `init_tlvs`
2. types:
    1. type: 1 (`networks`)
    2. data:
        * [`...*chain_hash`:`chains`]
    1. type: 3 (`remote_addr`)
    2. data:
        * [`...*byte`:`data`]

オプションの `networks` は、ノードが関心を持っているチェーンを示します。
オプションの `remote_addr` は NAT の問題を回避するために使用できます。

#### 要件

送信ノードは:
  - どの接続でも、最初の Lightning メッセージとして `init` を送信しなければなりません。
  - 機能ビットを [BOLT #9](09-features.md) で定義されたとおりに設定しなければなりません。
  - 未定義の機能ビットは 0 に設定しなければなりません。
  - `globalfeatures` には 13 を超える機能を設定すべきではありません。
  - `features` フィールドの表現に必要な最小の長さを使用すべきです。
  - ゴシップまたはチャネル開設の対象とするすべてのチェーンを `networks` に設定すべきです。
  - 自身が接続の受信側であり、接続が IP 経由である場合、受信接続のリモート IP アドレス (およびポート) を反映するように `remote_addr` を設定すべきです。
  - `remote_addr` を設定する場合:
    - [BOLT 7](07-routing-gossip.md#the-node_announcement-message) で説明されている、有効な `address descriptor` (1 バイトの type とデータ) に設定しなければなりません。
    - `remote_addr` にプライベートアドレスを設定すべきではありません。

受信ノードは:
  - 他のメッセージを送信する前に、`init` の受信を待たなければなりません。
  - 2 つの機能ビットマップをビット単位 OR で結合し、1 つの論理的な `features` マップにしなければなりません。
  - 既知の機能ビットに対して、[BOLT #9](09-features.md) で指定されているとおりに応答しなければなりません。
  - ゼロでない未知の _奇数_ 機能ビットを受信した場合:
    - そのビットを無視しなければなりません。
  - ゼロでない未知の _偶数_ 機能ビットを受信した場合:
    - 接続を閉じなければなりません。
  - 共通のチェーンを 1 つも含まない `networks` を受信した場合:
    - 接続を閉じてよいです。
  - 機能ベクタが既知の推移的依存関係をすべて満たしていない場合:
    - 接続を閉じなければなりません。
  - `remote_addr` を用いて自身の `node_announcement` を更新してよいです。

#### 設計上の根拠

以前はここに 2 つの機能ビットフィールドがありましたが、後方互換性のために現在は 1 つに統合されています。

このセマンティクスにより、将来の非互換な変更と後方互換な変更の双方が可能になります。ビットは原則としてペアで割り当てられるべきで、これによりオプションだった機能を後に必須化できるようになります。

ノードが互いの機能を待ち合わせるのは、機能が非互換だった場合のエラー診断を容易にするためです。

すべてのネットワークが同じポートを共有しているにもかかわらず、ほとんどの実装は単一のネットワークしかサポートしていません。そのため `networks` フィールドは、ノードが希望するネットワークの更新を受け取れる、あるいはチャネルを開けると誤解する事態を防ぎます。

### `error` と `warning` メッセージ

診断を容易にするために、何かが正しくないことをピアに伝えると便利な場面がしばしばあります。

1. type: 17 (`error`)
2. data:
   * [`channel_id`:`channel_id`]
   * [`u16`:`len`]
   * [`len*byte`:`data`]

1. type: 1 (`warning`)
2. data:
   * [`channel_id`:`channel_id`]
   * [`u16`:`len`]
   * [`len*byte`:`data`]

#### 要件

チャネルは `channel_id` で参照されます。ただし `channel_id` が 0 (すなわち全バイトが 0) の場合は、すべてのチャネルを指します。

チャネル確立 v1 (`open_channel`) を使用する funding ノードは:
  - `funding_created` メッセージまでに送信される (それを含む) すべてのエラーメッセージについて:
    - `channel_id` の代わりに `temporary_channel_id` を使用しなければなりません。

チャネル確立 v1 (`accept_channel`) を使用する fundee ノードは:
  - `funding_signed` メッセージより前に送信される (それを含まない) すべてのエラーメッセージについて:
    - `channel_id` の代わりに `temporary_channel_id` を使用しなければなりません。

チャネル確立 v2 (`open_channel2`) を使用するオープナーノードは:
  - `accept_channel2` メッセージを受信する前に送信されるすべてのエラーメッセージについて:
    - `channel_id` の代わりに `temporary_channel_id` を使用しなければなりません。

チャネル確立 v2 (`open_channel2`) を使用するアクセプタノードは:
  - `accept_channel2` メッセージまでに送信される (それを含む) すべてのエラーメッセージについて:
    - `channel_id` の代わりに `temporary_channel_id` を使用しなければなりません。

送信ノードは:
  - プロトコル違反、チャネルを使用不能にする内部エラー、または以後の通信を不能にするエラーが発生した場合は、`error` を送信すべきです。
  - 未知のチャネルに関する type `32`-`255` のメッセージに対する応答として、その未知の `channel_id` を入れた `error` を送信すべきです。
  - `error` を送信する際:
    - エラーメッセージで参照されるチャネルを失敗させなければなりません。
    - すべてのチャネルを示すために `channel_id` を全ゼロに設定してよいです。
  - `warning` を送信する際:
    - 警告が特定のチャネルに紐付かない場合、`channel_id` を全ゼロにしてよいです。
  - `data` フィールドは空でもかまいません。
  - 失敗が署名検証の失敗によって生じた場合:
    - `funding_created`、`funding_signed`、`closing_signed`、`commitment_signed` のいずれかのメッセージに対する応答として、生の 16 進エンコードされたトランザクションを含めるべきです。

受信ノードは:
  - `error` を受信した場合:
    - `channel_id` が全ゼロのとき:
      - 送信ノードとのすべてのチャネルを失敗させなければなりません。
    - そうでないとき:
      - 該当チャネルが送信ノードとのものである場合、`channel_id` で参照されるチャネルを失敗させなければなりません。
  - `warning` を受信した場合:
    - 後の診断のためにメッセージを記録すべきです。
    - その時点で許される場合、`shutdown` を試みてよいです。
  - `channel_id` で参照される既存のチャネルが存在しない場合:
    - メッセージを無視しなければなりません。
  - `data` が印刷可能な ASCII 文字 (参考: 印刷可能文字集合はバイト値 32 から 126 を含む) のみで構成されていない場合:
    - `data` をそのまま出力すべきではありません。

#### 設計上の根拠

回復不能なエラーが起きた場合、会話を中断する必要があります。単に接続を切ってしまうと、ピアが再接続を試みるおそれがあります。また、プロトコル違反を診断のために伝えるのも有用で、これは一方のピアにバグがあることを示します。

一方で、エラーメッセージの濫用は (さもなくば高価なチャネルの破棄を避けるために) 実装側がエラーを無視する事態を招いたため、軽微なエラーに対しては再試行や回復の余地を残せるよう、`warning` メッセージが追加されました。

本番環境では、情報漏洩を避けるためにエラーを細かく区別しないほうが賢明な場合もあります。そのために `data` フィールドはオプションになっています。

## 制御メッセージ

### `ping` と `pong` メッセージ

長時間維持される TCP 接続を成り立たせるために、両端がアプリケーションレベルで TCP 接続を生かし続ける必要が生じることがあります。これらのメッセージはトラフィックパターンの難読化にも役立ちます。

1. type: 18 (`ping`)
2. data:
    * [`u16`:`num_pong_bytes`]
    * [`u16`:`byteslen`]
    * [`byteslen*byte`:`ignored`]

`pong` メッセージは、`ping` メッセージを受信するたびに送信されます。これは応答として機能するとともに、受信者がまだ生きていることを明示的に伝えつつ接続を維持する役割も果たします。受信側は、受信した `ping` メッセージの中で送信者が指定したバイト数を `pong` メッセージのデータペイロードに含めます。

1. type: 19 (`pong`)
2. data:
    * [`u16`:`byteslen`]
    * [`byteslen*byte`:`ignored`]

#### 要件

`ping` メッセージを送信するノードは:
  - `ignored` を 0 で埋めるべきです。
  - `ignored` に秘密情報や初期化済みメモリの一部のような機微なデータを入れてはなりません。
  - 対応する `pong` を受信しない場合:
    - ネットワーク接続を閉じてよいですが、
      - この場合チャネルを失敗させてはなりません。

`pong` メッセージを送信するノードは:
  - `ignored` を 0 で埋めるべきです。
  - `ignored` に秘密情報や初期化済みメモリの一部のような機微なデータを入れてはなりません。

`ping` メッセージを受信するノードは:
  - `num_pong_bytes` が 65532 未満の場合:
    - `byteslen` を `num_pong_bytes` と等しくした `pong` メッセージを応答として送信しなければなりません。
  - それ以外の場合 (`num_pong_bytes` が 65532 未満で**ない**場合):
    - その `ping` を無視しなければなりません。

`pong` メッセージを受信するノードは:
  - `byteslen` が、自身が送信したいずれの `ping` の `num_pong_bytes` 値とも対応しない場合:
    - 接続を閉じてよいです。

### 設計上の根拠

メッセージの最大サイズは 65535 バイトです。したがって `byteslen` の現実的な上限は 65531 となります (これは type フィールド (`pong`) と `byteslen` 自体の分を差し引いた値です)。この値は、応答を送らせたくない場合の `num_pong_bytes` のしきい値として都合よく利用できます。

ネットワーク内のノード間の接続は長期にわたる場合があります。ペイメントチャネルの寿命に上限がないためです。しかし、接続の存続期間のかなりの部分では新しいデータが交換されないことも珍しくありません。さらにプラットフォームによっては、Lightning クライアントが事前通知なしにスリープに入る可能性もあります。そのため、接続の相手側の生存性を確認し、確立した接続を維持するために、専用の `ping` メッセージが使われます。

加えて、送信側が受信側に特定のバイト数の応答を要求できるおかげで、ネットワーク上のノードは _合成_ トラフィックを生成できます。このようなトラフィックを使えば、各チャネルに実際の更新を加えることなく典型的なやり取りのトラフィックパターンを模倣できるため、パケット解析やタイミング解析に対する部分的な防御として活用できます。

[BOLT #4](04-onion-routing.md) で定義されるオニオンルーティングプロトコルと組み合わせ、統計的に丁寧に設計された合成トラフィックを流せば、ネットワーク参加者のプライバシーをさらに高められます。

`ping` フラッディングへの最低限の対策は推奨されますが、ネットワーク遅延を考慮してある程度の余地は残しておきます。受信側に対するトラフィックフラッディングには他の手段もある (たとえば _奇数_ の未知メッセージ type を送る、すべてのメッセージを最大長までパディングするなど) 点には注意してください。

最後に、定期的な `ping` メッセージは、[BOLT #8](08-transport.md) で規定される鍵ローテーションを頻繁に促す効果もあります。

## ピアストレージ

### `peer_storage` と `peer_storage_retrieval` メッセージ

`option_provide_storage` 機能をアドバタイズしているノードは、ピアのために任意のデータを保管するサービスを提供します。保管するデータは、Lightning メッセージに収められるよう 65531 バイトを超えてはなりません。

ノードは再接続のたびに、取り出されたデータの内容を自分が最後に送ったものと比較することで、`option_provide_storage` を持つピアが正しく自分のデータを保管しているかを確認できます。ただし、ピアが常に最新のデータを保持していることまでを期待すべきではありません。

ノードは `peer_storage` メッセージを使ってピアにデータの保管を依頼し、ピアからは `peer_storage_retrieval` メッセージで最新のデータを返してもらうことを期待します:

1. type: 7 (`peer_storage`)
2. data:
    * [`u16`:`length`]
    * [`length*byte`:`blob`]


1. type: 9 (`peer_storage_retrieval`)
2. data:
    * [`u16`:`length`]
    * [`length*byte`:`blob`]


要件:

`peer_storage` の送信者は:
  - 必要に応じていつでも `peer_storage` を送信してよいです。
  - `blob` を 65531 バイトに収めなければなりません。
  - 受信時に完全性 (integrity) が確認できるように、データを暗号化しなければなりません。
  - `blob` の長さが常に厳密に 65531 バイトになるようにパディングすべきです。

`peer_storage` の受信者は:
  - 自身が `option_provide_storage` を提供している場合:
    - 送信者との間にチャネルが開いているならば:
      - メッセージを保管しなければなりません。
    - そうでない場合でも、保管してよいです。

- メッセージを保管する場合:
  - ピアの更新を 1 分につき 1 回までにレート制限するため、保管を遅延させてよいです。
  - 古い `blob` を、最後に受信したものに置き換えなければなりません。
  - 再接続後、`init` メッセージを交換したあとに、`peer_storage_retrieval` を再度送信しなければなりません。


`peer_storage_retrieval` の送信者は:
  - そのピアのために最後に保管した `blob` を含めなければなりません。
  - そのピアとのすべてのチャネルが閉じられた場合:
    - `blob` を削除する前に、少なくとも 2016 ブロック待つべきです。

`peer_storage_retrieval` の受信者は:
  - 古い、または無関係なデータを含む `peer_storage_retrieval` を受信した場合:
    - warning を送ってよいです。

#### 設計上の根拠:
`peer_storage` と `peer_storage_retrieval` メッセージは、ノードがネットワーク上の他のノードと安全にデータを保管・共有することを可能にし、重要情報のバックアップ手段として機能します。これらを利用することで、ノードは重要なデータを保護でき、ネットワーク全体の耐障害性と信頼性を高められます。さらに、チャネルが開かれていない場合でも、対価としていくらかの sats を受け取って `peer_storage` を保管するサービスを提供するノードがあるかもしれません。

`blob` の実サイズを隠すためにパディングを入れることで、サイズに基づく解析を受信者にとって難しくし、プライバシーを向上させます。

`peer_storage_retrieval` は `channel_reestablish` の後に送信されるべきではありません。そうしてしまうと、ユーザーがデータを失った場合にノードを復旧して状態を更新する選択肢を失ってしまうためです。

ピアに保管された `blob` を更新したいときはいつでも、`peer_storage` メッセージを送るとよいでしょう。この `blob` は暗号化済みデータの配布にも使え、ノードの復旧に役立てられます。

## 付録 A: BigSize テストベクトル

以下のテストベクタは、TLV フォーマットで使用する BigSize 実装の正確性を検証するために利用できます。このフォーマットは bitcoin で用いられている CompactSize エンコーディングと同一ですが、マルチバイト値のエンコーディングをリトルエンディアンからビッグエンディアンに変えています。

BigSize でエンコードされた値は、整数のサイズに応じて 1、3、5、9 バイトのいずれかのエンコーディングを生成します。エンコーディングは `uint64` 値 `x` を入力として、次のように振る舞う区分関数です:
```
        uint8(x)                if x < 0xfd
        0xfd + be16(uint16(x))  if x < 0x10000
        0xfe + be32(uint32(x))  if x < 0x100000000
        0xff + be64(x)          otherwise.
```

ここで `+` は連結を表し、`be16`、`be32`、`be64` はそれぞれ 16、32、64 ビット整数の入力をビッグエンディアンでエンコードします。

ある値について、それより少ないバイト数でエンコードできないとき、その値は _最小限にエンコードされている (minimally encoded)_ と言います。たとえば値が 0x10000 未満であるのに 5 バイトの BigSize エンコーディングを使っている場合、それは最小限にはエンコードされていません。BigSize でデコードされたすべての値は、最小限にエンコードされているかを確認すべきです。

### BigSize デコードテスト

以下は、BigSize デコードテストの実行例です。
```golang
func testReadBigSize(t *testing.T, test bigSizeTest) {
        var buf [8]byte 
        r := bytes.NewReader(test.Bytes)
        val, err := tlv.ReadBigSize(r, &buf)
        if err != nil && err.Error() != test.ExpErr {
                t.Fatalf("expected decoding error: %v, got: %v",
                        test.ExpErr, err)
        }

        // If we expected a decoding error, there's no point checking the value.
        if test.ExpErr != "" {
                return
        }

        if val != test.Value {
                t.Fatalf("expected value: %d, got %d", test.Value, val)
        }
}
```

正しい実装は、以下のテストベクタに対してすべて合格するはずです:
```json
[
    {
        "name": "zero",
        "value": 0,
        "bytes": "00"
    },
    {
        "name": "one byte high",
        "value": 252,
        "bytes": "fc"
    },
    {
        "name": "two byte low",
        "value": 253,
        "bytes": "fd00fd"
    },
    {
        "name": "two byte high",
        "value": 65535,
        "bytes": "fdffff"
    },
    {
        "name": "four byte low",
        "value": 65536,
        "bytes": "fe00010000"
    },
    {
        "name": "four byte high",
        "value": 4294967295,
        "bytes": "feffffffff"
    },
    {
        "name": "eight byte low",
        "value": 4294967296,
        "bytes": "ff0000000100000000"
    },
    {
        "name": "eight byte high",
        "value": 18446744073709551615,
        "bytes": "ffffffffffffffffff"
    },
    {
        "name": "two byte not canonical",
        "value": 0,
        "bytes": "fd00fc",
        "exp_error": "decoded bigsize is not canonical"
    },
    {
        "name": "four byte not canonical",
        "value": 0,
        "bytes": "fe0000ffff",
        "exp_error": "decoded bigsize is not canonical"
    },
    {
        "name": "eight byte not canonical",
        "value": 0,
        "bytes": "ff00000000ffffffff",
        "exp_error": "decoded bigsize is not canonical"
    },
    {
        "name": "two byte short read",
        "value": 0,
        "bytes": "fd00",
        "exp_error": "unexpected EOF"
    },
    {
        "name": "four byte short read",
        "value": 0,
        "bytes": "feffff",
        "exp_error": "unexpected EOF"
    },
    {
        "name": "eight byte short read",
        "value": 0,
        "bytes": "ffffffffff",
        "exp_error": "unexpected EOF"
    },
    {
        "name": "one byte no read",
        "value": 0,
        "bytes": "",
        "exp_error": "EOF"
    },
    {
        "name": "two byte no read",
        "value": 0,
        "bytes": "fd",
        "exp_error": "unexpected EOF"
    },
    {
        "name": "four byte no read",
        "value": 0,
        "bytes": "fe",
        "exp_error": "unexpected EOF"
    },
    {
        "name": "eight byte no read",
        "value": 0,
        "bytes": "ff",
        "exp_error": "unexpected EOF"
    }
]
```

### BigSize エンコードテスト

以下は、BigSize エンコードテストの実行例です。
```golang
func testWriteBigSize(t *testing.T, test bigSizeTest) {
        var (
                w   bytes.Buffer
                buf [8]byte
        )
        err := tlv.WriteBigSize(&w, test.Value, &buf)
        if err != nil {
                t.Fatalf("unable to encode %d as bigsize: %v",
                        test.Value, err)
        }

        if bytes.Compare(w.Bytes(), test.Bytes) != 0 {
                t.Fatalf("expected bytes: %v, got %v",
                        test.Bytes, w.Bytes())
        }
}
```

正しい実装は、以下のテストベクタに対してすべて合格するはずです:
```json
[
    {
        "name": "zero",
        "value": 0,
        "bytes": "00"
    },
    {
        "name": "one byte high",
        "value": 252,
        "bytes": "fc"
    },
    {
        "name": "two byte low",
        "value": 253,
        "bytes": "fd00fd"
    },
    {
        "name": "two byte high",
        "value": 65535,
        "bytes": "fdffff"
    },
    {
        "name": "four byte low",
        "value": 65536,
        "bytes": "fe00010000"
    },
    {
        "name": "four byte high",
        "value": 4294967295,
        "bytes": "feffffffff"
    },
    {
        "name": "eight byte low",
        "value": 4294967296,
        "bytes": "ff0000000100000000"
    },
    {
        "name": "eight byte high",
        "value": 18446744073709551615,
        "bytes": "ffffffffffffffffff"
    }
]
```

## 付録 B: Type-Length-Value テストベクトル

以下のテストは、2 つの別々の TLV 名前空間 n1 と n2 が存在することを前提とします。

n1 名前空間は、次の TLV type をサポートします:

1. `tlv_stream`: `n1`
2. types:
   1. type: 1 (`tlv1`)
   2. data:
     * [`tu64`:`amount_msat`]
   1. type: 2 (`tlv2`)
   2. data:
     * [`short_channel_id`:`scid`]
   1. type: 3 (`tlv3`)
   2. data:
     * [`point`:`node_id`]
     * [`u64`:`amount_msat_1`]
     * [`u64`:`amount_msat_2`]
   1. type: 254 (`tlv4`)
   2. data:
     * [`u16`:`cltv_delta`]

n2 名前空間は、次の TLV type をサポートします:

1. `tlv_stream`: `n2`
2. types:
   1. type: 0 (`tlv1`)
   2. data:
     * [`tu64`:`amount_msat`]
   1. type: 11 (`tlv2`)
   2. data:
     * [`tu32`:`cltv_expiry`]

### TLV デコード失敗

どの名前空間でも、次の TLV ストリームはデコード失敗となるはずです:

1. 無効なストリーム: 0xfd
2. 理由: type が途中で切れている

1. 無効なストリーム: 0xfd01
2. 理由: type が途中で切れている

1. 無効なストリーム: 0xfd0001 00
2. 理由: type が最小限にエンコードされていない

1. 無効なストリーム: 0xfd0101
2. 理由: length が欠落している

1. 無効なストリーム: 0x0f fd
2. 理由: (length が途中で切れている)

1. 無効なストリーム: 0x0f fd26
2. 理由: (length が途中で切れている)

1. 無効なストリーム: 0x0f fd2602
2. 理由: value が欠落している

1. 無効なストリーム: 0x0f fd0001 00
2. 理由: length が最小限にエンコードされていない

1. 無効なストリーム: 0x0f fd0201 
2. 理由: value が途中で切れている

どちらの名前空間でも、次の TLV ストリームはデコード失敗となるはずです:

1. 無効なストリーム: 0x12 00
2. 理由: 未知の偶数 type。

1. 無効なストリーム: 0xfd0102 00
2. 理由: 未知の偶数 type。

1. 無効なストリーム: 0xfe01000002 00
2. 理由: 未知の偶数 type。

1. 無効なストリーム: 0xff0100000000000002 00
2. 理由: 未知の偶数 type。

名前空間 `n1` での次の TLV ストリームは、デコード失敗となるはずです:

1. 無効なストリーム: 0x01 09 ffffffffffffffffff
2. 理由: `n1` の `tlv1` のエンコーディング長を超えている。

1. 無効なストリーム: 0x01 01 00
2. 理由: `n1` の `tlv1` の `amount_msat` のエンコーディングが最小限でない

1. 無効なストリーム: 0x01 02 0001
2. 理由: `n1` の `tlv1` の `amount_msat` のエンコーディングが最小限でない

1. 無効なストリーム: 0x01 03 000100
2. 理由: `n1` の `tlv1` の `amount_msat` のエンコーディングが最小限でない

1. 無効なストリーム: 0x01 04 00010000
2. 理由: `n1` の `tlv1` の `amount_msat` のエンコーディングが最小限でない

1. 無効なストリーム: 0x01 05 0001000000
2. 理由: `n1` の `tlv1` の `amount_msat` のエンコーディングが最小限でない

1. 無効なストリーム: 0x01 06 000100000000
2. 理由: `n1` の `tlv1` の `amount_msat` のエンコーディングが最小限でない

1. 無効なストリーム: 0x01 07 00010000000000
2. 理由: `n1` の `tlv1` の `amount_msat` のエンコーディングが最小限でない

1. 無効なストリーム: 0x01 08 0001000000000000
2. 理由: `n1` の `tlv1` の `amount_msat` のエンコーディングが最小限でない

1. 無効なストリーム: 0x02 07 01010101010101
2. 理由: `n1` の `tlv2` のエンコーディング長より短い。

1. 無効なストリーム: 0x02 09 010101010101010101
2. 理由: `n1` の `tlv2` のエンコーディング長より長い。

1. 無効なストリーム: 0x03 21 023da092f6980e58d2c037173180e9a465476026ee50f96695963e8efe436f54eb
2. 理由: `n1` の `tlv3` のエンコーディング長より短い。

1. 無効なストリーム: 0x03 29 023da092f6980e58d2c037173180e9a465476026ee50f96695963e8efe436f54eb0000000000000001
2. 理由: `n1` の `tlv3` のエンコーディング長より短い。

1. 無効なストリーム: 0x03 30 023da092f6980e58d2c037173180e9a465476026ee50f96695963e8efe436f54eb000000000000000100000000000001
2. 理由: `n1` の `tlv3` のエンコーディング長より短い。

1. 無効なストリーム: 0x03 31 043da092f6980e58d2c037173180e9a465476026ee50f96695963e8efe436f54eb00000000000000010000000000000002
2. 理由: `n1` の `node_id` が有効な点 (point) ではない。

1. 無効なストリーム: 0x03 32 023da092f6980e58d2c037173180e9a465476026ee50f96695963e8efe436f54eb0000000000000001000000000000000001
2. 理由: `n1` の `tlv3` のエンコーディング長より長い。

1. 無効なストリーム: 0xfd00fe 00
2. 理由: `n1` の `tlv4` のエンコーディング長より短い。

1. 無効なストリーム: 0xfd00fe 01 01
2. 理由: `n1` の `tlv4` のエンコーディング長より短い。

1. 無効なストリーム: 0xfd00fe 03 010101
2. 理由: `n1` の `tlv4` のエンコーディング長より長い。

1. 無効なストリーム: 0x00 00
2. 理由: `n1` 名前空間における未知の偶数フィールド。

### TLV デコード成功

どちらの名前空間でも、以下の TLV ストリームは正しくデコードされ、無視されるはずです:

1. 有効なストリーム: 0x
2. 説明: 空メッセージ

1. 有効なストリーム: 0x21 00
2. 説明: 未知の奇数 type。

1. 有効なストリーム: 0xfd0201 00
2. 説明: 未知の奇数 type。

1. 有効なストリーム: 0xfd00fd 00
2. 説明: 未知の奇数 type。

1. 有効なストリーム: 0xfd00ff 00
2. 説明: 未知の奇数 type。

1. 有効なストリーム: 0xfe02000001 00
2. 説明: 未知の奇数 type。

1. 有効なストリーム: 0xff0200000000000001 00
2. 説明: 未知の奇数 type。

名前空間 `n1` での以下の TLV ストリームは、ここで示す値で正しくデコードされるはずです:

1. 有効なストリーム: 0x01 00
2. 値: `tlv1` `amount_msat`=0

1. 有効なストリーム: 0x01 01 01
2. 値: `tlv1` `amount_msat`=1

1. 有効なストリーム: 0x01 02 0100
2. 値: `tlv1` `amount_msat`=256

1. 有効なストリーム: 0x01 03 010000
2. 値: `tlv1` `amount_msat`=65536

1. 有効なストリーム: 0x01 04 01000000
2. 値: `tlv1` `amount_msat`=16777216

1. 有効なストリーム: 0x01 05 0100000000
2. 値: `tlv1` `amount_msat`=4294967296

1. 有効なストリーム: 0x01 06 010000000000
2. 値: `tlv1` `amount_msat`=1099511627776

1. 有効なストリーム: 0x01 07 01000000000000
2. 値: `tlv1` `amount_msat`=281474976710656

1. 有効なストリーム: 0x01 08 0100000000000000
2. 値: `tlv1` `amount_msat`=72057594037927936

1. 有効なストリーム: 0x02 08 0000000000000226
2. 値: `tlv2` `scid`=0x0x550

1. 有効なストリーム: 0x03 31 023da092f6980e58d2c037173180e9a465476026ee50f96695963e8efe436f54eb00000000000000010000000000000002
2. 値: `tlv3` `node_id`=023da092f6980e58d2c037173180e9a465476026ee50f96695963e8efe436f54eb `amount_msat_1`=1 `amount_msat_2`=2

1. 有効なストリーム: 0xfd00fe 02 0226
2. 値: `tlv4` `cltv_delta`=550

### TLV ストリームのデコード失敗

有効なストリームの末尾に無効なストリームを連結したものは、デコード失敗を引き起こすはずです。

番号の小さい方の有効なストリームの末尾に、番号の大きい方の有効なストリームを連結しても、デコード失敗にはならないはずです。

加えて、名前空間 `n1` での以下の TLV ストリームはデコード失敗となるはずです:

1. 無効なストリーム: 0x02 08 0000000000000226 01 01 2a
2. 理由: 有効な TLV レコードだが、順序が不正

1. 無効なストリーム: 0x02 08 0000000000000231 02 08 0000000000000451
2. 理由: TLV type の重複

1. 無効なストリーム: 0x1f 00 0f 01 2a
2. 理由: 有効 (無視される) な TLV レコードだが、順序が不正

1. 無効なストリーム: 0x1f 00 1f 01 2a
2. 理由: TLV type の重複 (無視される側)

名前空間 `n2` での次の TLV ストリームは、デコード失敗となるはずです:

1. 無効なストリーム: 0xffffffffffffffffff 00 00 00
2. 理由: 有効な TLV レコードだが、順序が不正

## 付録 C: メッセージ拡張 {/*appendix-c-message-extension*/}

このセクションでは、`init` メッセージにおける有効/無効な拡張の例を示します。これらの例で土台となる (拡張なしの) `init` メッセージは `0x001000000000` (すべての機能がオフ) です。

以下の `init` メッセージは有効です:

- `0x001000000000`: 拡張なし
- `0x001000000000c9012acb0104`: 拡張に 2 つの未知の _奇数_ TLV レコード (type `0xc9` と `0xcb`) が含まれている

以下の `init` メッセージは無効です:

- `0x00100000000001`: 拡張は存在するが途中で切れている
- `0x001000000000ca012a`: 拡張に未知の _偶数_ TLV レコードが含まれている (TLV type `0xca` を未知と仮定)
- `0x001000000000c90101c90102`: 拡張の TLV ストリームが不正 (TLV レコード type `0xc9` が重複)

なお、メッセージが署名される場合、_extension_ も署名対象のバイトに含まれます。ノードは、たとえ内容を理解できなくても、署名を正しく検証できるよう _extension_ バイトを保存しておくべきです。

## 付録 D: 符号付き整数のテストベクトル {/*appendix-d-signed-integers-test-vectors*/}

以下のテストベクタは、符号付き整数 (`s8`、`s16`、`s32`、`s64`) がビッグエンディアンの 2 の補数表現でどのようにエンコードされるかを示します。

```json
[
    {
        "value": 0,
        "bytes": "00"
    },
    {
        "value": 42,
        "bytes": "2a"
    },
    {
        "value": -42,
        "bytes": "d6"
    },
    {
        "value": 127,
        "bytes": "7f"
    },
    {
        "value": -128,
        "bytes": "80"
    },
    {
        "value": 128,
        "bytes": "0080"
    },
    {
        "value": -129,
        "bytes": "ff7f"
    },
    {
        "value": 15000,
        "bytes": "3a98"
    },
    {
        "value": -15000,
        "bytes": "c568"
    },
    {
        "value": 32767,
        "bytes": "7fff"
    },
    {
        "value": -32768,
        "bytes": "8000"
    },
    {
        "value": 32768,
        "bytes": "00008000"
    },
    {
        "value": -32769,
        "bytes": "ffff7fff"
    },
    {
        "value": 21000000,
        "bytes": "01406f40"
    },
    {
        "value": -21000000,
        "bytes": "febf90c0"
    },
    {
        "value": 2147483647,
        "bytes": "7fffffff"
    },
    {
        "value": -2147483648,
        "bytes": "80000000"
    },
    {
        "value": 2147483648,
        "bytes": "0000000080000000"
    },
    {
        "value": -2147483649,
        "bytes": "ffffffff7fffffff"
    },
    {
        "value": 500000000000,
        "bytes": "000000746a528800"
    },
    {
        "value": -500000000000,
        "bytes": "ffffff8b95ad7800"
    },
    {
        "value": 9223372036854775807,
        "bytes": "7fffffffffffffff"
    },
    {
        "value": -9223372036854775808,
        "bytes": "8000000000000000"
    }
]
```

## 謝辞 {/*acknowledgments*/}

[ TODO: (roasbeef); fin ]

## 参考文献 {/*references*/}

1. <a id="reference-1">http://www.unicode.org/charts/PDF/U2600.pdf</a>

## 著者 {/*authors*/}

[ FIXME: Insert Author List ]

![Creative Commons License](https://i.creativecommons.org/l/by/4.0/88x31.png "License CC-BY")
<br>
この作品は [Creative Commons Attribution 4.0 International License](http://creativecommons.org/licenses/by/4.0/) の下でライセンスされています。
